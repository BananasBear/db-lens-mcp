# Standards

本目录记录稳定开发规范和规则约束。

## 当前规范

- `engineering.md`：工程实现规范。
- `domain-design.md`：领域模型、模块封装和边界规范。
- `security.md`：安全边界和危险操作规则。
- `testing.md`：测试规范。
- `documentation.md`：文档维护规范。

## 使用规则

- 影响所有模块的约束写在这里。
- 只影响单个模块的设计写到 `../modules/`。
- 安全规则变化必须同步检查模块设计和测试规范。
